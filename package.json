{
    "name": "rails-react-list",
    "engines": {
        "node": "6.3.1"
    },
    "scripts": {
        "build": "cd client && npm install && npm run build && cd ..",
        "deploy": "cp -a client/build/. public/",
        "postinstall": "npm run build  --prefix client && npm run deploy --prefix client && echo 'Client built!'"
    }
}